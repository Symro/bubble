<div class="index-container">
    <div class="logo"></div>
    <header>
        <!-- <a href="">INSCRIPTION.HTML</a> -->
    </header>
    <section class="w50 large-w33 small-w75 tiny-w75 ">
        <form class="form-signin" method='POST' action='/user/createUser'>
        <h2 class="form-signin-heading">Créer un compte</h2>

        <div class="form-error">
            <% if(flash && flash.err) { %>
            <ul class="alert alert-success">
            <% Object.keys(flash.err).forEach(function(error) { %>
                <li><%= flash.err[error].message %></li>
            <% }) %>
            </ul>
            <% } %>
        </div>

        <input name='firstname' type="text" class="input-block-level" placeholder="Prénom" value="" ><br />

        <input name='mail' type="email" class="input-block-level" placeholder="Adresse email" value=""><br />


        <input id="password" name='password' type="password" class="input-block-level" placeholder="Mot de passe" ><br />

        <input name='confirmation' type="password" class="input-block-level" placeholder="Confirmation mot de passe" ><br />

        <input type='submit' class="btn btn-large" id="validinscription"  value="C'est parti">

        <input type="hidden" name="_csrf" value="<%= _csrf %>" />

        <a href='/session/new'>Retour</a>
        </form>

    </section>